
pub enum OrderType {

    /// Market order.
    Market,

    /// Limit order.
    Limit,

    /// Stop loss market order.
    StopLoss,

    /// Stop loss limit order.
    StopLossLimit,

    /// Take profit market order.
    TakeProfit,

    /// Take profit limit order.
    TakeProfitLimit,

}